<p style="color: green"><%= notice %></p>

<h1>Games</h1>

<div id="games">
  <% @games.each do |game| %>
    <div id="<%= dom_id(game) %>">
      <p>
        <strong>Name:</strong>
        <%= game.name %>
      </p>
    </div>
    <p>
      <%# link_to "Show this game", game %>
    </p>
  <% end %>
</div>

<%= link_to "New game", new_game_path %>
